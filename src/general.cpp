#include "general.h"

#include <string>
#include <iostream>

using namespace std;

GeneralHeader::GeneralHeader(string &src, int &index)
{
	init(src, index);
}

void GeneralHeader::init(string &src, int &index)
{

}
